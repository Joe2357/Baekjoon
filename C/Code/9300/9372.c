#include <stdio.h>

int main() {
	int t;
	scanf("%d", &t);
	while (t--) {
		int n, m;
		scanf("%d %d", &n, &m);
		while (m--) {
			int a, b;
			scanf("%d %d", &a, &b);
		}
		printf("%d\n", --n);
	}
	return 0;
}